# ADR-010: DP-JIN-SVG-DETERMINISM-01 案 B: 出力直前に固定桁数へ丸める関数を 1 本通す規約にする（桁数は未決）

> ⚠️ **AI 仮決定（ai_provisional）** — auto mode の仮判断であり人間確定ではない（DP-AUTOMODE-01）。PR レビュー後、`/decide` で approved / overridden に確定する。

- **ステータス**: proposed (ai_provisional)
- **日付**: 2026-09-04
- **決定者**: auto-decider
- **関連判断ポイント**: DP-JIN-SVG-DETERMINISM-01

## コンテキスト

_（コンテキストは案件側で追記）_

## 選択肢

| 選択肢 | 採否 |
|---|---|
| 案 B: 出力直前に固定桁数へ丸める関数を 1 本通す規約にする（桁数は未決） | 採用 |
| 案 A: Python の float をそのまま f-string で文字列化する | 不採用 |
| 案 C: Decimal または整数座標系で計算する | 不採用 |

## 決定

design.yaml fired_decision_points[DP-JIN-SVG-DETERMINISM-01] の推奨案（案 B）をそのまま採用する。座標を SVG に書き出す直前に固定桁数へ丸め、その丸めを行う関数 1 本を必ず通す規約にする。要件との適合根拠: NFR-DET-001 が「同じ意味モデルからは常にバイト単位で同一の SVG」を要求し、要件書 §9 が「同一入力 2 回で完全一致」をテストするが、§2.5 のレイアウトは半径 R=1 の正規化座標での三角関数計算を経て px へ変換するため浮動小数の文字列化がそのままバイト列に現れる。案 A（float をそのまま f-string 化）は決定性が「たまたま同じ実装だから」に依存し担保として弱く、案 C（Decimal / 整数座標系）は §2.5 が三角関数の使用を前提にしているため噛み合わず実装が重い。丸め桁数の具体値は要件書に記載がないため本判断では確定しない（T-002）。confidence は medium とする。

## 影響

_（影響は案件側で追記）_
