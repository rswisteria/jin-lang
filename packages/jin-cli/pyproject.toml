[project]
name = "jin-cli"
version = "0.1.0"
description = "Jin の CLI（check / fmt / schema / dump / build / run / render）"
requires-python = ">=3.12"
# 版の根拠: delivery/20260904-1445-jin/version-matrix.md（2026-09-04 実測）
dependencies = [
  "jin-core",
  "jin-adk",
  "jin-render",
  "typer>=0.27,<1",
]

[project.scripts]
jin = "jin_cli.main:app"

[tool.uv.sources]
jin-core = { workspace = true }
jin-adk = { workspace = true }
jin-render = { workspace = true }

[build-system]
requires = ["hatchling>=1.32"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/jin_cli"]
